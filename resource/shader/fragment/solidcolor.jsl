#include frag.jsl

uniform vec3 color;

layout(location=15) FRAG_IN vec3 normal;

void main() {
  outputAlbedo(color);
  outputNormal(normalize(normal));
  outputMaterial(MATERIAL_NOSHADE)
  outputRoughness(0.);
}
